# 固定项目名，保证从仓库根目录或本目录启动都用同一套容器和数据卷
name: engram

services:
  engram-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: engram-frontend
    ports:
      - "5174:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
